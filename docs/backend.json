{
  "entities": {
    "Student": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Student",
      "type": "object",
      "description": "Represents a student enrolled in the professor's class, including their personal details for birthday reminders.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Student entity."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user (professor) this student belongs to."
        },
        "name": {
          "type": "string",
          "description": "The full name of the student."
        },
        "rollNumber": {
          "type": "string",
          "description": "The unique roll number assigned to the student within the institution or class."
        },
        "department": {
          "type": "string",
          "description": "The academic department to which the student belongs, e.g., 'Artificial Intelligence and Machine Learning', 'Computer Science', 'Designing'."
        },
        "section": {
          "type": "string",
          "description": "The section of the class the student is in, e.g., 'A', 'B'."
        },
        "birthday": {
          "type": "string",
          "description": "The student's birth date, used for birthday notifications and calendar display.",
          "format": "date"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The student's phone number for SMS reminders."
        },
        "photoUrl": {
          "type": "string",
          "description": "An optional URL pointing to the student's profile photo.",
          "format": "uri"
        },
        "imageHint": {
            "type": "string",
            "description": "A hint for AI image generation based on the student's photo."
        }
      },
      "required": [
        "userId",
        "name",
        "rollNumber",
        "department",
        "section",
        "birthday"
      ]
    }
  },
  "auth": {
    "providers": [
      "email"
    ]
  },
  "firestore": {
    "/users/{userId}/students/{studentId}": {
      "schema": {
        "$ref": "#/entities/Student"
      },
      "description": "Collection to store a user's student documents."
    }
  }
}
